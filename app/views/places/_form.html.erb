<%= form_with(model: place) do |form| %>
<% if place.errors.any? %>
<div id="error_explanation">
    <h2><%= pluralize(place.errors.count, "error") %> prohibited this place from being saved:</h2>

    <ul>
        <% place.errors.each do |error| %>
        <li><%= error.full_message %></li>
        <% end %>
    </ul>
</div>
<% end %>
<div class="card-body">
    <div class="container">
        <div class="row">
            <div class="col-sm col-lg-5">
                <div class="field form-group">
                    <%= form.label :place_name %>
                    <%= form.text_field :place_name, class:"form-control" %>
                </div>
            </div>
            <div class="col-sm col-lg-5">
                <div class="field form-group">
                    <%= form.label :node_name %>
                    <%= form.text_field :place_name, class:"form-control" %>
                </div>
            </div>
        </div>
        <div class="row">
            <div class="col-sm col-lg-10">
                <div class="field form-group">
                    <%= form.label :Company %>
                    <!--collection_select(object, method, collection, value_method, text_method)
    <select name="object[method]" id="object_method">
    <option value="value_method">text_method</option>
    <option value="value_method">text_method</option>
    !-->
                    <%= collection_select( :place, :company_id, Company.all, :id, :name, {:prompt=>true},{:class=>"form-select"})%>
                </div>
                <!--%= form.collection_select :company_name, @Company,:id,%!-->
            </div>
            <div class="actions">
                <%= form.submit class:"btn btn-dark" %>
            </div>
        </div>
    </div>
</div>
<% end %>